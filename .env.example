# Public site URL, without a trailing slash. Used for canonical links, the sitemap and JSON-LD.
NEXT_PUBLIC_SITE_URL=https://robsonx4.github.io

# Leave empty: the site is served from the domain root, not a subdirectory.
NEXT_PUBLIC_BASE_PATH=
